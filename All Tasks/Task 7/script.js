function changeImageSrc() {
    // Selecting the image element by its ID
    let image = document.getElementById("myImage");
    
    // Changing the src attribute of the image element
    image.setAttribute("src", "/img/new-image.jpg");
}